services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL}
    container_name: tourism-frontend
    ports:
      - '80:8080'
    env_file:
      - .env

networks:
  tourism-network:
    external: true
